/*

  The file is empty on purpose!

  Here's why:

  Flex uses a non-standard include file, unistd.h, which is not available on Windows systems. 
  In the base dir is an empty unistd.h file; include the base dir in your include files path 
  (in MSVC: Tools->Options->Directories->Include).

  Need more help? Check out the readme.txt file

*/
